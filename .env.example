# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# =============================================================================
# Database (Development: Docker PostgreSQL / Production: Vercel Postgres)
# =============================================================================
# The database URL for PostgreSQL
# For local dev, run: pnpm --filter @portfolio/db dev-db:start
PORTFOLIO_DATABASE_URL="postgresql://portfolio:portfolio_secret@localhost:5432/portfolio_dev"

# =============================================================================
# Authentication (Clerk)
# =============================================================================
# Clerk publishable key (client-side)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."

# Clerk secret key (server-side)
CLERK_SECRET_KEY="sk_test_..."

# Clerk webhook secret for user sync
# @see https://clerk.com/docs/integrations/webhooks
CLERK_WEBHOOK_SECRET="whsec_..."

# =============================================================================
# Redis (Upstash)
# =============================================================================
# Upstash Redis REST URL
# @see https://upstash.com/docs/redis/overall/getstarted
UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"

# Upstash Redis REST token
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# =============================================================================
# AI / LLM (OpenAI)
# =============================================================================
# OpenAI API key for sentiment analysis
# @see https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# =============================================================================
# Market Data API (Twelve Data)
# =============================================================================
# Twelve Data - unified stocks + crypto (Free tier: 800 credits/day)
# Get your API key at https://twelvedata.com/
TWELVEDATA_API_KEY="your-api-key-here"

# =============================================================================
# Cron Jobs
# =============================================================================
# Secret for authenticating cron job requests
# Generate via: openssl rand -base64 32
CRON_SECRET="your-cron-secret"
